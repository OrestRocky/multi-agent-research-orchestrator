class DataCollectorAgent:
    def collect(self, query: str):
        print(f"[DATA COLLECTOR] Collecting data for query: {query}")
        # Placeholder: you can integrate APIs, scrape, or use datasets
        return f"Raw data related to: {query}"
